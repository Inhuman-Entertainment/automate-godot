## Tasks
- [ ] Add upstream detection to `calendar_adapter.gd` with an `is_available()` check that never errors when the addon is absent.
- [ ] Add configurable epoch date and interval unit to the `AutomateGodot` facade.
- [ ] Add interval-to-date and date-to-interval conversion helpers.
- [ ] Report completion dates for jobs given remaining intervals.
- [ ] Vendor `Godot4xCalendarButton` into the sample project or document its installation, respecting its MIT license.
- [ ] Add headless tests for inert-when-missing behavior and date mapping.
- [ ] Document the calendar integration and its fail-safe behavior in the README.
