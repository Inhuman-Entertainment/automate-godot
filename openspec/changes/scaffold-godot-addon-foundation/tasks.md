## Tasks
- [x] Create the repository with LICENSE, .gitignore, and a sample `project.godot` for opening the addon in Godot.
- [x] Add `addons/automate_godot/plugin.cfg` and `plugin.gd` registering the `AutomateGodot` autoload on enable and removing it on disable.
- [x] Add the `AutomateGodot` runtime facade with `ADDON_ID`, `VERSION`, `get_foundations()`, and `get_user_stories()`.
- [x] Add `integrations/calendar_adapter.gd` recording `Godot4xCalendarButton` as the calendar foundation.
- [x] Add `integrations/crafting_adapter.gd` recording `Godot-Systems` as the crafting architectural baseline.
- [x] Document the chosen foundations, trade-offs, and user-story direction in the README.
- [x] Add the addon/project icon.
